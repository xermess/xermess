# The Loginer API's public listener: the provider and the account API.
# The browser never uses this: it calls the API on this app's own origin, and
# the dev server's proxy (or the reverse proxy in production) routes the API
# paths here. The server-side renderer calls it directly.
API_URL=http://localhost:8080
